<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>F Society Encrypter by Tux</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-image: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1920 1080"><rect width="1920" height="1080" fill="%23000814"/><text x="50" y="30" fill="%2300ff41" font-family="monospace" font-size="12" opacity="0.8">setAttribute("checked", "checked");</text><text x="400" y="50" fill="%23ff6b6b" font-family="monospace" font-size="11" opacity="0.7">function createDocumentFragment() {</text><text x="800" y="80" fill="%234ecdc4" font-family="monospace" font-size="10" opacity="0.6">appendChild(lastChild);</text><text x="1200" y="110" fill="%23f7b731" font-family="monospace" font-size="12" opacity="0.8">onclick="check();"</text><text x="100" y="140" fill="%235f27cd" font-family="monospace" font-size="11" opacity="0.7">removeChild(0);</text><text x="600" y="170" fill="%2300ff41" font-family="monospace" font-size="10" opacity="0.6">padding:0;margin:0;border:0;</text><text x="1000" y="200" fill="%23ff6b6b" font-family="monospace" font-size="12" opacity="0.8">visibility:hidden;border:none;</text><text x="200" y="230" fill="%234ecdc4" font-family="monospace" font-size="11" opacity="0.7">height:0;width:0;</text><text x="700" y="260" fill="%23f7b731" font-family="monospace" font-size="10" opacity="0.6">display:block;overflow:hidden;</text><text x="1100" y="290" fill="%235f27cd" font-family="monospace" font-size="12" opacity="0.8">margin-top:1px;position:absolute;</text><text x="300" y="320" fill="%2300ff41" font-family="monospace" font-size="11" opacity="0.7">width="4px" createElement("div");</text><text x="800" y="350" fill="%23ff6b6b" font-family="monospace" font-size="10" opacity="0.6">insertBefore(firstChild, null);</text><text x="50" y="380" fill="%234ecdc4" font-family="monospace" font-size="12" opacity="0.8">jQuery(document).ready(function() {</text><text x="500" y="410" fill="%23f7b731" font-family="monospace" font-size="11" opacity="0.7">var data = getData();</text><text x="900" y="440" fill="%235f27cd" font-family="monospace" font-size="10" opacity="0.6">if (typeof nodeType !== "undefined") {</text><text x="150" y="470" fill="%2300ff41" font-family="monospace" font-size="12" opacity="0.8">return hasData(elem) ? data : {};</text><text x="650" y="500" fill="%23ff6b6b" font-family="monospace" font-size="11" opacity="0.7">function expando() { return expando; }</text><text x="1050" y="530" fill="%234ecdc4" font-family="monospace" font-size="10" opacity="0.6">nodeName.toLowerCase() === "input";</text><text x="250" y="560" fill="%23f7b731" font-family="monospace" font-size="12" opacity="0.8">setAttribute("class", "data-attrs");</text><text x="750" y="590" fill="%235f27cd" font-family="monospace" font-size="11" opacity="0.7">length ? getAttribute(name) : null;</text><text x="1150" y="620" fill="%2300ff41" font-family="monospace" font-size="10" opacity="0.6">queue.shift(); clearTimeout();</text><text x="350" y="650" fill="%23ff6b6b" font-family="monospace" font-size="12" opacity="0.8">onclick="dequeue()" string="true";</text><text x="850" y="680" fill="%234ecdc4" font-family="monospace" font-size="11" opacity="0.7">className = elem.className || "";</text><text x="450" y="710" fill="%23f7b731" font-family="monospace" font-size="10" opacity="0.6">padding:0;margin:0;border:0;display:none;</text><text x="950" y="740" fill="%235f27cd" font-family="monospace" font-size="12" opacity="0.8">visibility:hidden;border:0;width:0;</text><text x="50" y="770" fill="%2300ff41" font-family="monospace" font-size="11" opacity="0.7">height:0;position:absolute;top:0;</text><text x="550" y="800" fill="%23ff6b6b" font-family="monospace" font-size="10" opacity="0.6">left:-999px;border:1px;display:block;</text><text x="1050" y="830" fill="%234ecdc4" font-family="monospace" font-size="12" opacity="0.8">box-sizing:border-box;-moz-box-sizing;</text><text x="150" y="860" fill="%23f7b731" font-family="monospace" font-size="11" opacity="0.7">overflow:hidden;clip:rect(0 0 0 0);</text><text x="650" y="890" fill="%235f27cd" font-family="monospace" font-size="10" opacity="0.6">style.width="4px";createElement("div");</text><text x="1150" y="920" fill="%2300ff41" font-family="monospace" font-size="12" opacity="0.8">marginRight = style.marginRight;</text><text x="250" y="950" fill="%23ff6b6b" font-family="monospace" font-size="11" opacity="0.7">style.display="block";width="1px";</text><text x="750" y="980" fill="%234ecdc4" font-family="monospace" font-size="10" opacity="0.6">appendChild(div);removeChild();</text><text x="450" y="1010" fill="%23f7b731" font-family="monospace" font-size="12" opacity="0.8">padding:1px;border:1px;display:block;</text><text x="950" y="1040" fill="%235f27cd" font-family="monospace" font-size="11" opacity="0.7">position:absolute;border-box;sizing;</text></svg>');
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center;
            background-attachment: fixed;
            color: #00ff00;
            font-family: 'Orbitron', monospace;
            min-height: 100vh;
            overflow-x: hidden;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 30% 20%, rgba(255, 107, 107, 0.1) 0%, transparent 30%),
                radial-gradient(circle at 70% 80%, rgba(78, 205, 196, 0.1) 0%, transparent 30%),
                radial-gradient(circle at 20% 80%, rgba(95, 39, 205, 0.1) 0%, transparent 30%),
                radial-gradient(circle at 80% 20%, rgba(0, 255, 65, 0.1) 0%, transparent 30%),
                linear-gradient(135deg, rgba(0, 8, 20, 0.9) 0%, rgba(0, 20, 40, 0.8) 100%);
            z-index: -2;
        }

        .matrix-bg {
            display: none;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 20px;
            position: relative;
            z-index: 1;
        }

        .title {
            text-align: center;
            font-size: 2.5rem;
            font-weight: 900;
            margin-bottom: 10px;
            text-shadow: 0 0 20px #00ff00;
            animation: rainbow 3s linear infinite;
        }

        .subtitle {
            text-align: center;
            font-size: 1.2rem;
            margin-bottom: 30px;
            color: #ff6b6b;
            text-shadow: 0 0 10px #ff6b6b;
        }

        @keyframes rainbow {
            0% { filter: hue-rotate(0deg); }
            100% { filter: hue-rotate(360deg); }
        }

        .ascii-art {
            font-family: 'Courier New', monospace;
            font-size: 0.6rem;
            text-align: center;
            margin: 20px 0;
            background: linear-gradient(45deg, #ff0000, #ff7f00, #ffff00, #00ff00, #0000ff, #4b0082, #9400d3);
            background-size: 400% 400%;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: rainbow-flow 3s ease-in-out infinite;
            line-height: 1.2;
        }

        @keyframes rainbow-flow {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .input-section {
            background: rgba(0, 255, 0, 0.1);
            border: 2px solid #00ff00;
            border-radius: 15px;
            padding: 25px;
            margin: 30px 0;
            backdrop-filter: blur(10px);
            box-shadow: 0 0 30px rgba(0, 255, 0, 0.3);
        }

        .input-label {
            font-size: 1.3rem;
            margin-bottom: 15px;
            text-align: center;
            animation: glow 2s ease-in-out infinite alternate;
        }

        @keyframes glow {
            from { text-shadow: 0 0 5px #00ff00; }
            to { text-shadow: 0 0 20px #00ff00, 0 0 30px #00ff00; }
        }

        .input-box {
            width: 100%;
            padding: 15px;
            font-size: 1.1rem;
            background: rgba(0, 0, 0, 0.8);
            color: #00ff00;
            border: 2px solid #00ff00;
            border-radius: 10px;
            font-family: 'Orbitron', monospace;
            outline: none;
            transition: all 0.3s ease;
        }

        .input-box:focus {
            box-shadow: 0 0 20px #00ff00;
            border-color: #00ffff;
        }

        .encrypt-btn {
            width: 100%;
            padding: 15px;
            font-size: 1.2rem;
            font-weight: bold;
            background: linear-gradient(45deg, #ff0000, #00ff00, #0000ff);
            color: #000;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            margin-top: 15px;
            font-family: 'Orbitron', monospace;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .encrypt-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 0 30px rgba(255, 255, 255, 0.5);
        }

        .encrypt-btn:active {
            transform: scale(0.98);
        }

        .results {
            margin-top: 30px;
        }

        .result-item {
            background: rgba(0, 0, 0, 0.8);
            border-radius: 10px;
            padding: 20px;
            margin: 15px 0;
            border-left: 5px solid;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.5);
            transition: all 0.3s ease;
        }

        .result-item:hover {
            transform: translateX(10px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.7);
        }

        .result-title {
            font-size: 1.1rem;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .result-text {
            font-family: 'Courier New', monospace;
            font-size: 1rem;
            background: rgba(255, 255, 255, 0.1);
            padding: 15px;
            border-radius: 5px;
            word-break: break-all;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .result-text:hover {
            background: rgba(255, 255, 255, 0.2);
        }

        .caesar { border-left-color: #ff6b6b; }
        .caesar .result-title { color: #ff6b6b; }

        .base64 { border-left-color: #4ecdc4; }
        .base64 .result-title { color: #4ecdc4; }

        .xor { border-left-color: #45b7d1; }
        .xor .result-title { color: #45b7d1; }

        .hex { border-left-color: #f7b731; }
        .hex .result-title { color: #f7b731; }

        .lua-obfuscation { border-left-color: #e74c3c; }
        .lua-obfuscation .result-title { color: #e74c3c; }

        .advanced-xor { border-left-color: #9b59b6; }
        .advanced-xor .result-title { color: #9b59b6; }

        .bytecode { border-left-color: #e67e22; }
        .bytecode .result-title { color: #e67e22; }

        .multi-layer { border-left-color: #2ecc71; }
        .multi-layer .result-title { color: #2ecc71; }

        .octal { border-left-color: #3498db; }
        .octal .result-title { color: #3498db; }

        .copy-btn {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.3);
            color: #fff;
            padding: 5px 10px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 0.8rem;
            float: right;
            margin-top: -10px;
            transition: all 0.3s ease;
        }

        .copy-btn:hover {
            background: rgba(255, 255, 255, 0.2);
        }

        .hidden {
            display: none;
        }

        .notification {
            position: fixed;
            top: 20px;
            right: 20px;
            background: rgba(0, 255, 0, 0.9);
            color: #000;
            padding: 10px 20px;
            border-radius: 5px;
            font-weight: bold;
            z-index: 1000;
            transform: translateX(400px);
            transition: transform 0.3s ease;
        }

        .notification.show {
            transform: translateX(0);
        }

        @media (max-width: 768px) {
            .title { font-size: 1.8rem; }
            .ascii-art { font-size: 0.4rem; }
            .container { padding: 10px; }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="title">F SOCIETY ENCRYPTER</div>
        <div class="subtitle">by Tux</div>
        
        <div class="ascii-art">
███████╗    ███████╗ ██████╗  ██████╗██╗███████╗████████╗██╗   ██╗<br>
██╔════╝    ██╔════╝██╔═══██╗██╔════╝██║██╔════╝╚══██╔══╝╚██╗ ██╔╝<br>
█████╗      ███████╗██║   ██║██║     ██║█████╗     ██║    ╚████╔╝<br>
██╔══╝      ╚════██║██║   ██║██║     ██║██╔══╝     ██║     ╚██╔╝<br>
██║         ███████║╚██████╔╝╚██████╗██║███████╗   ██║      ██║<br>
╚═╝         ╚══════╝ ╚═════╝  ╚═════╝╚═╝╚══════╝   ╚═╝      ╚═╝<br>
<br>
███████╗███╗   ██╗ ██████╗██████╗ ██╗   ██╗██████╗ ████████╗███████╗██████╗<br>
██╔════╝████╗  ██║██╔════╝██╔══██╗╚██╗ ██╔╝██╔══██╗╚══██╔══╝██╔════╝██╔══██╗<br>
█████╗  ██╔██╗ ██║██║     ██████╔╝ ╚████╔╝ ██████╔╝   ██║   █████╗  ██████╔╝<br>
██╔══╝  ██║╚██╗██║██║     ██╔══██╗  ╚██╔╝  ██╔═══╝    ██║   ██╔══╝  ██╔══██╗<br>
███████╗██║ ╚████║╚██████╗██║  ██║   ██║   ██║        ██║   ███████╗██║  ██║<br>
╚══════╝╚═╝  ╚═══╝ ╚═════╝╚═╝  ╚═╝   ╚═╝   ╚═╝        ╚═╝   ╚══════╝╚═╝  ╚═╝
        </div>

        <div class="input-section">
            <div class="input-label">What text do you want to encrypt/protect?</div>
            <textarea class="input-box" id="textInput" placeholder="Enter your secret message here..." rows="4"></textarea>
            <button class="encrypt-btn" onclick="encryptText()">🔐 ENCRYPT MESSAGE 🔐</button>
        </div>

        <div class="results hidden" id="results">
            <div class="result-item caesar">
                <div class="result-title">🔐 Caesar Cipher (ROT13)</div>
                <button class="copy-btn" onclick="copyToClipboard('caesarResult')">Copy</button>
                <div class="result-text" id="caesarResult"></div>
            </div>

            <div class="result-item base64">
                <div class="result-title">🔐 Base64 Encoding</div>
                <button class="copy-btn" onclick="copyToClipboard('base64Result')">Copy</button>
                <div class="result-text" id="base64Result"></div>
            </div>

            <div class="result-item xor">
                <div class="result-title">🔐 XOR Encryption (Hex)</div>
                <button class="copy-btn" onclick="copyToClipboard('xorResult')">Copy</button>
                <div class="result-text" id="xorResult"></div>
            </div>

            <div class="result-item hex">
                <div class="result-title">🔐 Hexadecimal Encoding</div>
                <button class="copy-btn" onclick="copyToClipboard('hexResult')">Copy</button>
                <div class="result-text" id="hexResult"></div>
            </div>

            <div class="result-item lua-obfuscation">
                <div class="result-title">🔐 Lua-Style Obfuscation</div>
                <button class="copy-btn" onclick="copyToClipboard('luaResult')">Copy</button>
                <div class="result-text" id="luaResult"></div>
            </div>

            <div class="result-item advanced-xor">
                <div class="result-title">🔐 Advanced XOR (Multi-Key)</div>
                <button class="copy-btn" onclick="copyToClipboard('advancedXorResult')">Copy</button>
                <div class="result-text" id="advancedXorResult"></div>
            </div>

            <div class="result-item bytecode">
                <div class="result-title">🔐 Bytecode-Style Encoding</div>
                <button class="copy-btn" onclick="copyToClipboard('bytecodeResult')">Copy</button>
                <div class="result-text" id="bytecodeResult"></div>
            </div>

            <div class="result-item multi-layer">
                <div class="result-title">🔐 Multi-Layer Encryption</div>
                <button class="copy-btn" onclick="copyToClipboard('multiLayerResult')">Copy</button>
                <div class="result-text" id="multiLayerResult"></div>
            </div>

            <div class="result-item octal">
                <div class="result-title">🔐 Octal Encoding</div>
                <button class="copy-btn" onclick="copyToClipboard('octalResult')">Copy</button>
                <div class="result-text" id="octalResult"></div>
            </div>
        </div>
    </div>

    <div class="notification" id="notification">Copied to clipboard!</div>

    <script>
        // Create animated code background
        function createFloatingCode() {
            const codeElements = [
                'setAttribute("checked", "checked");',
                'function createDocumentFragment() {',
                'appendChild(lastChild);',
                'onclick="check();"',
                'removeChild(0);',
                'padding:0;margin:0;border:0;',
                'visibility:hidden;border:none;',
                'height:0;width:0;',
                'display:block;overflow:hidden;',
                'margin-top:1px;position:absolute;',
                'width="4px" createElement("div");',
                'insertBefore(firstChild, null);',
                'jQuery(document).ready(function() {',
                'var data = getData();',
                'if (typeof nodeType !== "undefined") {',
                'return hasData(elem) ? data : {};',
                'function expando() { return expando; }',
                'nodeName.toLowerCase() === "input";',
                'setAttribute("class", "data-attrs");',
                'length ? getAttribute(name) : null;',
                'queue.shift(); clearTimeout();',
                'onclick="dequeue()" string="true";',
                'className = elem.className || "";',
                'padding:0;margin:0;border:0;display:none;',
                'visibility:hidden;border:0;width:0;',
                'height:0;position:absolute;top:0;',
                'left:-999px;border:1px;display:block;',
                'box-sizing:border-box;-moz-box-sizing;',
                'overflow:hidden;clip:rect(0 0 0 0);',
                'style.width="4px";createElement("div");',
                'marginRight = style.marginRight;',
                'style.display="block";width="1px";',
                'appendChild(div);removeChild();'
            ];

            const colors = ['#00ff41', '#ff6b6b', '#4ecdc4', '#f7b731', '#5f27cd'];
            
            for(let i = 0; i < 15; i++) {
                const codeElement = document.createElement('div');
                codeElement.textContent = codeElements[Math.floor(Math.random() * codeElements.length)];
                codeElement.style.position = 'fixed';
                codeElement.style.fontSize = Math.random() * 6 + 8 + 'px';
                codeElement.style.color = colors[Math.floor(Math.random() * colors.length)];
                codeElement.style.opacity = Math.random() * 0.3 + 0.1;
                codeElement.style.fontFamily = 'monospace';
                codeElement.style.left = Math.random() * window.innerWidth + 'px';
                codeElement.style.top = Math.random() * window.innerHeight + 'px';
                codeElement.style.pointerEvents = 'none';
                codeElement.style.zIndex = '-1';
                codeElement.style.transform = 'rotate(' + (Math.random() * 20 - 10) + 'deg)';
                codeElement.style.transition = 'all ' + (Math.random() * 20 + 10) + 's linear';
                
                document.body.appendChild(codeElement);
                
                // Animate the element
                setTimeout(() => {
                    codeElement.style.transform = 'translateY(' + (window.innerHeight + 100) + 'px) rotate(' + (Math.random() * 360) + 'deg)';
                    codeElement.style.opacity = '0';
                }, 100);
                
                // Remove element after animation
                setTimeout(() => {
                    if(codeElement.parentNode) {
                        codeElement.parentNode.removeChild(codeElement);
                    }
                }, (Math.random() * 20 + 10) * 1000);
            }
        }

        // Create floating code elements periodically
        createFloatingCode();
        setInterval(createFloatingCode, 3000);

        // Advanced obfuscation functions
        function generateRandomNumbers() {
            const nums = [];
            for(let i = 0; i < 50; i++) {
                nums.push(Math.floor(Math.random() * 1000000) + (Math.random() > 0.5 ? 1 : -1) * Math.floor(Math.random() * 500000));
            }
            return nums;
        }

        function octalEncode(text) {
            let result = "";
            for(let i = 0; i < text.length; i++) {
                result += "\\" + text.charCodeAt(i).toString(8).padStart(3, '0');
            }
            return result;
        }

        function luaStyleObfuscation(text) {
            const chars = [];
            const nums = generateRandomNumbers();
            
            // Convert to octal-style encoding like the Lua code
            for(let i = 0; i < text.length; i++) {
                const code = text.charCodeAt(i);
                const obfuscated = code + nums[i % nums.length];
                chars.push(obfuscated.toString(8).padStart(3, '0'));
            }
            
            // Create nested structure similar to Lua obfuscation
            let result = "local N={";
            for(let i = 0; i < chars.length; i++) {
                result += `"\\${chars[i]}"`;
                if(i < chars.length - 1) result += ",";
                if((i + 1) % 8 === 0 && i < chars.length - 1) result += "\n";
            }
            result += "}";
            
            // Add complex mathematical operations
            const operations = [];
            for(let i = 0; i < 10; i++) {
                const a = Math.floor(Math.random() * 1000000);
                const b = Math.floor(Math.random() * 1000000);
                operations.push(`${a}+${b}`);
                operations.push(`${a}-${b}`);
                operations.push(`${a}*${Math.floor(Math.random() * 100)}`);
            }
            
            result += `\nlocal function decode(i) return N[i+(${operations[0]})] end`;
            result += `\nfor i=${operations[1]},#N,${operations[2]} do`;
            result += `\n  if type(N[i])=="string" then`;
            result += `\n    local encoded=N[i]:gsub("\\\\(%d+)",function(n) return string.char(tonumber(n,8)-${nums[0]}) end)`;
            result += `\n    N[i]=encoded`;
            result += `\n  end`;
            result += `\nend`;
            
            return result;
        }

        function advancedXorEncrypt(text, keys = ["FSOCIETY", "HACKTHE", "PLANET", "ANON"]) {
            let result = "";
            let keyIndex = 0;
            
            for(let i = 0; i < text.length; i++) {
                const currentKey = keys[keyIndex % keys.length];
                const char = text.charCodeAt(i);
                const keyChar = currentKey.charCodeAt(i % currentKey.length);
                const encrypted = char ^ keyChar ^ (i * 3) ^ (keyIndex * 7);
                result += encrypted.toString(16).padStart(4, '0');
                
                if(i % 5 === 0) keyIndex++;
            }
            
            return result;
        }

        function bytecodeStyleEncoding(text) {
            const encoded = [];
            const complexNums = generateRandomNumbers();
            
            for(let i = 0; i < text.length; i++) {
                const char = text.charCodeAt(i);
                const offset = complexNums[i % complexNums.length];
                const encoded1 = (char + offset) % 256;
                const encoded2 = Math.floor((char + offset) / 256);
                encoded.push(`${encoded1}+${encoded2}*256-${offset}`);
            }
            
            let result = "return(function(...)local decode={";
            for(let i = 0; i < encoded.length; i++) {
                result += encoded[i];
                if(i < encoded.length - 1) result += ";";
            }
            result += "}";
            result += "local function getString() local result='' ";
            result += "for i=1,#decode do result=result..string.char(decode[i]) end ";
            result += "return result end return getString()end)()";
            
            return result;
        }

        function multiLayerEncryption(text) {
            // Layer 1: XOR with multiple keys
            let layer1 = advancedXorEncrypt(text);
            
            // Layer 2: Octal encoding
            let layer2 = "";
            for(let i = 0; i < layer1.length; i++) {
                layer2 += "\\" + layer1.charCodeAt(i).toString(8).padStart(3, '0');
            }
            
            // Layer 3: Base64 with custom alphabet
            const customAlphabet = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
            const shuffledAlphabet = customAlphabet.split('').sort(() => Math.random() - 0.5).join('');
            
            let layer3 = btoa(layer2);
            let result = "";
            for(let i = 0; i < layer3.length; i++) {
                const index = customAlphabet.indexOf(layer3[i]);
                result += index !== -1 ? shuffledAlphabet[index] : layer3[i];
            }
            
            return `-- Encrypted with F Society Advanced Obfuscation\n${result}`;
        }

        // Original functions for compatibility
        function caesarCipher(text, shift = 13) {
            return text.replace(/[a-zA-Z]/g, function(char) {
                const start = char <= 'Z' ? 65 : 97;
                return String.fromCharCode(((char.charCodeAt(0) - start + shift) % 26) + start);
            });
        }

        function base64Encode(text) {
            try {
                return btoa(unescape(encodeURIComponent(text)));
            } catch(e) {
                return "Error encoding";
            }
        }

        function xorEncrypt(text, key = "FSOCIETY") {
            let result = "";
            for(let i = 0; i < text.length; i++) {
                result += String.fromCharCode(text.charCodeAt(i) ^ key.charCodeAt(i % key.length));
            }
            return result;
        }

        function toHex(text) {
            let result = "";
            for(let i = 0; i < text.length; i++) {
                result += text.charCodeAt(i).toString(16).padStart(2, '0');
            }
            return result;
        }

        function encryptText() {
            const input = document.getElementById('textInput').value;
            
            if(!input.trim()) {
                alert('Please enter some text to encrypt!');
                return;
            }

            // Caesar Cipher
            const caesarResult = caesarCipher(input, 13);
            document.getElementById('caesarResult').textContent = caesarResult;

            // Base64
            const base64Result = base64Encode(input);
            document.getElementById('base64Result').textContent = base64Result;

            // XOR Encryption
            const xorResult = xorEncrypt(input);
            const xorHex = toHex(xorResult);
            document.getElementById('xorResult').textContent = xorHex;

            // Hexadecimal
            const hexResult = toHex(input);
            document.getElementById('hexResult').textContent = hexResult;

            // Lua-Style Obfuscation
            const luaResult = luaStyleObfuscation(input);
            document.getElementById('luaResult').textContent = luaResult;

            // Advanced XOR with multiple keys
            const advancedXorResult = advancedXorEncrypt(input);
            document.getElementById('advancedXorResult').textContent = advancedXorResult;

            // Bytecode-Style Encoding
            const bytecodeResult = bytecodeStyleEncoding(input);
            document.getElementById('bytecodeResult').textContent = bytecodeResult;

            // Multi-Layer Encryption
            const multiLayerResult = multiLayerEncryption(input);
            document.getElementById('multiLayerResult').textContent = multiLayerResult;

            // Octal Encoding
            const octalResult = octalEncode(input);
            document.getElementById('octalResult').textContent = octalResult;

            // Show results
            document.getElementById('results').classList.remove('hidden');
            
            // Smooth scroll to results
            document.getElementById('results').scrollIntoView({ behavior: 'smooth' });
        }

        function copyToClipboard(elementId) {
            const text = document.getElementById(elementId).textContent;
            navigator.clipboard.writeText(text).then(function() {
                showNotification();
            }).catch(function() {
                // Fallback for older browsers
                const textArea = document.createElement("textarea");
                textArea.value = text;
                document.body.appendChild(textArea);
                textArea.select();
                document.execCommand('copy');
                document.body.removeChild(textArea);
                showNotification();
            });
        }

        function showNotification() {
            const notification = document.getElementById('notification');
            notification.classList.add('show');
            setTimeout(() => {
                notification.classList.remove('show');
            }, 2000);
        }

        // Allow Enter key to encrypt
        document.getElementById('textInput').addEventListener('keypress', function(e) {
            if(e.key === 'Enter' && !e.shiftKey) {
                e.preventDefault();
                encryptText();
            }
        });

        // Resize canvas on window resize
        window.addEventListener('resize', function() {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
        });
    </script>
</body>
</html>
